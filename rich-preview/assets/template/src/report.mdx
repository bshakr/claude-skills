import { Hero, HighlightGrid, ComparisonGrid, Timeline, RiskList, ActionList, CompleteDocument } from "./components/editorial"
import { Mermaid } from "./components/mermaid"
import "./styles.css"

export const Report = ({ source }) => (
  <div className="preview-shell">
    <Hero
      eyebrow="Preview"
      title="Replace with the source title"
      lede="Replace with a one-sentence summary taken from the source."
      status="Draft"
    />
    {/*
      Author editorial sections here from references/components.md.
      Use only facts the source states. Delete this comment and the Hero
      placeholders once you have real content, then keep CompleteDocument last.
    */}
    <CompleteDocument source={source} />
  </div>
)

<Report {...props} />
